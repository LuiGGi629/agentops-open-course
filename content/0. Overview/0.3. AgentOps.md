---
title: "0.3. AgentOps"
description: Follow the six lifecycle phases the chapters are built from, see the artifact behind each one, and learn what closes the loop.
slug: "0-3-agentops"
---

{{% admonition abstract "In one glance" %}}

- **You will:** Follow the six lifecycle phases the course chapters are built from, and see the artifact behind each.
- **You need:** [0.1. Agents]({{< relref "/0. Overview/0.1. Agents.md" >}}) read, so you already know what the loop is.
- **Time:** about 8 minutes, concept. {{% /admonition %}}

## AgentOps begins after the demo works {#what-is-agentops}

The demo always works. The agent calls the right tool, writes a good paragraph, and everyone agrees this is going to production. Three weeks later the provider ships a minor version, the same prompt takes a different route through the tools, and nobody can say whether that route is better, worse, or quietly skipping the step that made the old one safe.

**AgentOps** is the practice of building, evaluating, securing, deploying, and operating agents as reliable production software — the agent-shaped sibling of MLOps and LLMOps. MLOps owns a model you train; LLMOps adds what you get when you consume one instead, from prompts to token cost; AgentOps adds what you get when the model can call your functions. The outer layer is what the autonomy costs you, and if you are still deciding whether you want that cost, the decision is one page back in [0.1. Agents]({{< relref "/0. Overview/0.1. Agents.md#when-should-you-not-use-an-agent" >}}).

## Six phases, and each one is a chapter

```mermaid
flowchart TD
    Build["Build<br/>compose/compose.go · model/model.go"]
    Capabilities["Capabilities<br/>tools · skills · MCP · memory"]
    Quality["Quality<br/>test gate · model-backed eval evidence"]
    Gateway["Gateway<br/>infra/agentgateway"]
    Platform["Platform<br/>infra/kagent · infra/k8s"]
    Observe["Observe<br/>telemetry/telemetry.go → OTLP → Tempo / Prometheus / Loki"]
    Build --> Capabilities --> Quality --> Gateway --> Platform --> Observe
    Observe -->|distil a trace into a regression case, re-run| Build
```

**Diagram in words:** The six phases run in a line — Build, Capabilities, Quality, Gateway, Platform, Observe — each naming the artifact that implements it. One edge closes the loop: what Observe records is distilled into a regression case and re-run from Build, which makes this a cycle rather than a checklist.

The table of contents _is_ that loop: [2. Agents]({{< relref "/2. Agents/_index.md" >}}), [3. Capabilities]({{< relref "/3. Capabilities/_index.md" >}}), [4. Quality]({{< relref "/4. Quality/_index.md" >}}), [5. Gateway]({{< relref "/5. Gateway/_index.md" >}}), [6. Platform]({{< relref "/6. Platform/_index.md" >}}), and [7. Observability]({{< relref "/7. Observability/_index.md" >}}), with [1. Setup]({{< relref "/1. Setup/_index.md" >}}) before them and [8. Community]({{< relref "/8. Community/_index.md" >}}) after.

No phase is a bullet you have to imagine; each is a directory you can open. Build is `compose/` and `model/`; Capabilities is `tools/`, `compose/skills.go`, `mcpserver/`, and `memory/`; Quality is the `*_test.go` suites, the root `evals/` module, and `policy/`. The last three are `infra/agentgateway/`, `infra/kagent/` with `infra/k8s/`, and `infra/observability/`; paths without an `infra/` prefix live under `agents/go/`. "Harden it" and "watch it" are two files here, `policy/guardrails.go` and `telemetry/telemetry.go`.

## What an agent adds to the operations problem

Compared with a stateless model endpoint, an agent introduces four failure modes, and each already ships a defense you can read rather than a warning you have to heed:

| The new failure mode                             | What the reference agent does about it                                                                                                                                            |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The same input can produce different tool calls  | Evaluations score the _trajectory_ over fixed seed data rather than exact strings ([4.4. Evaluations]({{< relref "/4. Quality/4.4. Evaluations.md" >}}))                          |
| A tool call can change state                     | Mutating tools pause for a human yes, then commit the change and an append-only audit row in one transaction ([4.5. Guardrails]({{< relref "/4. Quality/4.5. Guardrails.md" >}})) |
| Every loop step is another model call            | Per-session token accounting, with a configured budget blocking the next call once it is spent ([7.3. Costs]({{< relref "/7. Observability/7.3. Costs.md" >}}))                   |
| Untrusted tool output can hijack the instruction | Lookalike characters normalized, injection markers neutralized, free text spotlighted as data ([4.6. Security]({{< relref "/4. Quality/4.6. Security.md" >}}))                    |

Each chapter also states its own edge, because a defense described without its limit is marketing: the audit trail is append-only through SQLite triggers but not tamper-proof against an administrator, spotlighting is defense in depth rather than a guarantee, and the token budget bounds one conversation rather than one client.

## What closes the loop

The edge from Observe back to Build is easy to assert and easy to leave hollow. Three habits close it here, and none is an automated online scorer. First, a bad trace becomes a regression case: a wrong trajectory or an unsafe proposal is distilled into one evaluation case that names the behavior, and its structure is checked without spending a token.

```bash
mise run eval:validate
```

```text
[eval:validate] $ cd evals && mise run eval:validate
[eval:validate] $ go run ./cmd/agentops-eval validate
{
  "evalsets": 3,
  "cases": 21,
  "calibration_cases": 12
}
```

Second, every change re-runs the same three commands — `mise run format`, `mise run check`, `mise run test` — with the race detector on, and in the two Go modules the course teaches from, `agents/go` and `evals`, `test` also holds an 80% per-package line-coverage floor. Clearing that floor means the tests reach the code, which is a weaker claim than it sounds ([0.2. Evidence]({{< relref "/0. Overview/0.2. Evidence.md" >}})). Third, claims that rot on a calendar rather than a commit, such as pinned versions and model names, are re-checked by a scheduled freshness job that opens one tracking issue.

## What you can do now

- You can name the six phases in order and the chapter that owns each.
- You can name one artifact behind any two phases, such as `policy/guardrails.go` for Quality.
- You can name a shipped defense, and its stated limit, for at least two of the four failure modes an agent adds.
- You can describe the Observe-to-Build edge concretely: one bad trace becomes one validated evaluation case.

You now have the map the rest of the course walks. Hand it a problem someone describes at work — "it did something different this time", "it changed a record nobody approved", "the bill tripled" — and you can name the phase it belongs to and the chapter that owns it.

Continue to [0.4. Ecosystem]({{< relref "/0. Overview/0.4. Ecosystem.md" >}}) when you want to know which component owns which boundary.
