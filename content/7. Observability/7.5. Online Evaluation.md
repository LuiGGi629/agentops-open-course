---
title: "7.5. Online Evaluation"
description: Separate shipped offline Go evaluation from the sampled runtime scoring pipeline the course deliberately does not implement.
slug: "7-5-online-evaluation"
---

{{% admonition abstract "In one glance" %}}

- **You will:** Sample your own runtime traces, find out exactly how much they can and cannot prove about answer quality, and design the scorer you are not going to switch on.
- **You need:** [7.1. Tracing]({{< relref "/7. Observability/7.1. Tracing.md" >}}) finished, with its trace switch turned back on and a few turns of your own already in Tempo — with the shipped default in place, the agent records nothing and every search below comes back empty.
- **Time:** about 25 minutes, concept. {{% /admonition %}}

## Your offline suite is green and users are still unhappy

Twenty-one committed cases pass. The release went out. A week later support says the answers "feel worse," and every number you own says nothing changed — because every number you own was measured against inputs you wrote yourself, before anyone touched the system.

That is the gap online evaluation is supposed to close. Offline evaluation asks whether a candidate regressed _known_ behaviors against fixed, versioned cases. Online evaluation asks whether real inputs and outcomes are drifting beyond what that fixed set ever anticipated. They answer different questions, and only one of them has already run.

Look at what you actually have to work with. Ask Tempo for your own recent turns over a window you state explicitly:

```bash
now=$(date +%s)
curl -s "http://localhost:3200/api/search?tags=service.name%3Dagentops-agent&limit=5&start=$((now-14400))&end=$now" \
  | jq -r '.traces[]? | "\(.traceID) \(.rootTraceName) \(.durationMs)ms"'
```

```text
be014c35ffa2038f1b14d4fce3bd6034 invoke_agent agentops_agent 333925ms
f6310c8a5550385867bf607597572dc8 invoke_agent agentops_agent 33ms
3e8d2df81ff06371331e74d2505434d9 invoke_agent agentops_agent 30ms
525e677f26a6eb6433ac4fbd886fec5a invoke_agent agentops_agent 895571ms
7488083813448af2ab00db9c0649e1ee invoke_agent agentops_agent 181374ms
```

Five real turns off a four-hour window. State that window yourself, by the way — the search API defaults to roughly the last hour, so an empty result usually means your traces are older than the question you asked.

Now try to answer the support ticket from that. Two of these turns came back in about thirty milliseconds and three ran for minutes, one of them for a quarter of an hour. That is a genuine finding, and it tells you precisely nothing about whether any of the five answers was correct. The asymmetry is the whole lesson of this page.

## Can metadata tell you the answers got worse?

No, and the reason is structural rather than temporary. Content capture defaults to false, so runtime traces keep operation, model, tool, token, latency, status, and correlation metadata while excluding prompts, responses, and tool payloads. Without the answer and the rows it was built from, no later job can decide whether that answer was correct or grounded. It can only score coarse properties: tool-name order, status, latency, model selection.

There is no clever way around that. Enabling content capture duplicates user data into a durable store and changes your privacy, access, and retention obligations — which is precisely the decision [7.1. Tracing]({{< relref "/7. Observability/7.1. Tracing.md" >}}) makes you take deliberately, for one synthetic request, on fictional data. You do not get answer correctness from content-free telemetry for free.

What you can watch honestly are bounded proxies: request rate, latency, and error ratio by operation and model; guardrail injection-neutralization and gateway rejection counts; structured-report schema failures; model-call and token distributions; circuit openings and dependency errors. A changed proxy says something moved. It never says the answers got worse.

Detecting even that much honestly takes discipline. Choose a baseline window, a candidate window, a minimum sample size, and bounded dimensions in advance; confirm the source and model identity for _both_ windows before comparing them, using the tuple from [7.0. Reproducibility]({{< relref "/7. Observability/7.0. Reproducibility.md" >}}); and require a difference larger than sampling noise before acting, because lab traffic is sparse and noisy. Above all: the absence of a quality metric is not evidence of stable quality. When nothing is scoring content, the correct report is "no online correctness trend exists," not "quality is fine."

One population trap is worth closing before you sample anything. Evaluation runs use `service.name=agentops-evals` and stable `agentops.eval.*` signals, and they export only when `EVAL_OTEL_EXPORTER_OTLP_ENDPOINT` is set explicitly; the harness also forces child-agent telemetry off. Ask Tempo whether any of that traffic is in your window:

```bash
curl -s 'http://localhost:3200/api/search?tags=service.name%3Dagentops-evals&limit=5' | jq -c '.traces // []'
```

```text
[]
```

Empty, as it should be on a runtime sample. Estimating user drift from a population containing your own committed eval prompts would mean measuring your test suite and reporting it as customer behavior — the separation is architectural, but checking it is one command and takes two seconds.

**You now hold a defensible runtime sample: a stated window, your own traffic, and a one-command demonstration that your test suite is not hiding inside it. Most teams asking about drift cannot produce that much.**

What the course ships is runtime telemetry, offline model evaluation, and a path for promoting sanitized findings into committed cases. What it does not ship is a sampling rule, a consent mechanism, an online judge scheduler, reviewer access control, assessment storage, alert thresholds, or incident routing for answer quality. Do not claim drift detection is active.

That is not a gap left by laziness; it is eight controls that a production design has to earn:

1. A representative, rate-limited inclusion rule.
1. Consent, minimization, and retention for every field read.
1. Redaction before any external model call.
1. Versioned scorer and judge identities.
1. Cost, timeout, and error budgets.
1. Deduplicated verdict storage linked to the source trace without mutating it.
1. Sustained thresholds and accountable incident owners.
1. A path that promotes sanitized failures into offline cases.

And one structural rule that outranks the eight: run the job outside the request path, so scorer latency or a scorer outage cannot harm the user whose answer is being judged. An online scorer cannot retract an answer that has already been sent — request-time policy owns prevention, and asynchronous scoring can only detect and route.

## Your turn: sample your own traffic, then design the scorer

Write the one-page design. Not code — the design is what makes the missing controls visible before somebody switches something on.

Predict first: of the seven fields below, how many can you fill from telemetry you already have, and how many require a decision nobody has made yet?

- **Mode**: `inspect` — you read Tempo and write a page of your own; nothing in the repository changes.
- **Goal**: produce a bounded runtime sample you can defend, and a design that states plainly what it could and could not score.
- **Files to touch**: none in this repository. Keep the design wherever your team keeps designs.
- **Preflight**: confirm Tempo is ready with `curl --fail http://localhost:3200/ready`, and confirm the trace switch is still accepted in this shell with `echo $ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS` printing `true`. If the search window comes back empty, send a few turns with the agent running under those exports — a shipped-default agent will never fill it.
- **Steps**: run both search commands above over a window you state. Then fill seven fields: included and excluded traces; metadata or content fields read; redaction and retention; scorer version and cost cap; baseline, threshold, window, and minimum sample; verdict store and trace-link key; owner, alert route, and rollback action.
- **Gate that proves completion**: your sample returns your own runtime traces and an empty `agentops-evals` result over the same window, and every one of the seven fields has a concrete value — including an owner by name and a rollback action.
- **Final state**: no judge is running, no scorer is scheduled, and your design says in one sentence whether it can assess answer correctness at all. If it needs content, it names the privacy cost explicitly.

The sentence most designs get wrong is the last one. A scorer that reads only metadata is cheap, safe, and cannot tell you whether an answer was right; a scorer that reads content can, and has just become a system that stores user data. Write down which one you chose and why, because the person who inherits it will otherwise assume the expensive answer.

## What you can do now

- You pulled a bounded runtime sample out of Tempo over a window you stated, and proved it contains no evaluation traffic.
- You can say precisely what metadata-only traces can score and what they cannot, without hedging.
- You can list the controls a live scorer would need, and name the one structural rule that keeps it out of the request path.
- Your design has an owner, a rollback, and an honest sentence about answer correctness — and nothing is switched on.

"We should score production traffic" is a sentence that sounds like a feature request and is actually a privacy and consent design with a scoring problem attached. You can now say that out loud in a planning meeting, with your own sample on the screen to show what the cheap version would and would not have caught.

Continue to [7.6. Governance]({{< relref "/7. Observability/7.6. Governance.md" >}}), where the question stops being whether the answer was good and becomes who authorized what it did.
