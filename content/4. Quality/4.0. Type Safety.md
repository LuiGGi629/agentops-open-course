---
title: "4.0. Type Safety"
description: Turn untrusted model, environment, wire, and database values into typed Go values at the boundary that owns them, so an invalid state never travels.
slug: "4-0-type-safety"
---

{{% admonition abstract "In one glance" %}}

- **You will:** Watch a hostile tool argument die at the boundary that owns it, then trace the same discipline through configuration and structured answers.
- **You need:** Chapter 3 finished and `mise run doctor` passing. No model, no network.
- **Time:** about 18 minutes, concept. {{% /admonition %}}

## The tool argument that reads like an attack

Ana's agent has one job on the read side: look up `INC-002` and say what it found. But the argument that reaches `get_incident` is not typed by Ana. It is typed by a language model, which means the string can be anything — a valid id, a lowercase id with stray spaces, an id for an incident that does not exist, or something that was never an id at all.

Here is what the read surface does with three of those, and you can reproduce every one. Serve the reads over HTTP with `mise run mcp:http` from `agents/go`, then send from a second terminal the same `tools/call` request you used in [3.3. MCP]({{< relref "/3. Capabilities/3.3. MCP.md" >}}), changing only the argument:

```bash
curl -sN -X POST http://127.0.0.1:8000/mcp \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_incident","arguments":{"incident_id":"INC-2; DROP TABLE incidents"}}}' \
  | sed -n 's/^data: //p' | jq -c '.result.structuredContent'
```

Each block below is one such call's `structuredContent` object, indented for reading and otherwise unchanged:

```json
{ "error": "Invalid incident id \"INC-2; DROP TABLE incidents\"; expected an id like INC-002." }
```

```json
{ "error": "No incident found with id \"INC-999\"." }
```

Predict before you read the third one. The argument was `"  inc-002 "` — padded, lower-cased, the sort of thing a small model emits when it is paraphrasing rather than copying. Does the tool reject it as malformed, or does it answer?

```json
{
  "incident": {
    "id": "INC-002",
    "opened_at": "2026-07-05T09:02:00Z",
    "resolved_at": null,
    "runbook": "service-down",
    "service": "inventory",
    "severity": "SEV1",
    "status": "open",
    "summary": "Inventory pods are crash-looping; stock lookups return HTTP 503.",
    "title": "Inventory service unavailable"
  }
}
```

It answers, and it answers about `INC-002` — the id was trimmed and upper-cased before it was checked. Three arguments, three different outcomes, and none of them is a database error, a stack trace, or a leaked file path. That is the whole subject of this page: the difference between a value that was checked and a value that was _turned into something that cannot be wrong_.

## Parse at the edge, then trust the type

Go's compiler already proves a great deal before the binary starts. Values, methods, interfaces, and returns have to fit, so an entire family of missing-method and wrong-shape failures never reaches runtime. Prove it over every package whenever you doubt it:

```bash
cd agents/go
go build ./...
go test ./config ./domain ./tools -count=1
```

What compilation cannot prove is that the string in a `string` means anything. A model produces probabilistic text; a database row can be stale; an environment variable is whatever the operator typed. Decoding JSON into a struct moves that uncertainty around rather than removing it. The sequence that removes it looks like this:

```text
untrusted bytes -> strict decode -> normalize -> typed domain value -> operation
```

This is **parse, do not validate**: do the checking once, while constructing a narrower type, and then pass that type around. `domain.IncidentID` exists precisely so that no function downstream has to re-ask whether an id is shaped like an id. The only ways to obtain one are `ParseIncidentID`, which is strict because the seed's own rows must already be canonical, and `NormalizeIncidentID`, which trims and upper-cases first because `"  inc-002 "` is a plausible thing for a model to emit and worth accepting.

All three captures above came through the forgiving door: `runGetIncident` calls `NormalizeIncidentID` and nothing else. That is why the first and third differ — the padded lower-case id was made canonical and then checked, and the injection-shaped one had no canonical form to be made into. The strict door is the one the dataset's own rows come through, in the record decoder and the report decoder, and neither capture went near it. As for the second, `INC-999` failed no door at all. It is a perfectly good identifier with no row behind it, which is a different answer and deserves a different message.

Each boundary owns its own parser, and each fails at its own edge with a message naming the field or invariant it protects:

| Boundary            | Untrusted input            | Trusted result                                     |
| ------------------- | -------------------------- | -------------------------------------------------- |
| Environment         | Strings and missing values | `config.Config` with typed providers and durations |
| Model tool call     | JSON arguments             | Typed argument struct plus normalized domain ids   |
| A2A and REST        | Protocol JSON and metadata | Validated messages, tasks, and turns               |
| SQLite              | Rows and nullable columns  | Domain records or an explicit error                |
| Evaluation artifact | Captured protocol events   | Typed `evals.Turn` and sanitized result schema     |

Reach for a typed enum when the valid set is closed — provider, entrypoint, severity, incident status, A2A transport. Reach for a parsing constructor when a string has to satisfy a structure, such as an `INC-123` id or a kebab-case service slug. Reach for a struct when fields belong together and a half-built value would be ambiguous, and keep credentials in a wrapper whose ordinary formatting is already masked.

The last row of that table is worth dwelling on, because it is where the discipline pays off twice. When the agent is asked for a structured triage report, the evaluation harness decodes the answer with unknown fields refused, insists on exactly one JSON value, and then validates incident, severity, service, runbook, evidence, and action fields. It does that against its own `evals.TriageReport` type, not the agent's — the harness deliberately does not import `agents/go`. If both sides shared a type, one coordinated rename would let producer and grader agree on a regression without either of them noticing.

## Configuration is the same discipline, one hour earlier

A tool argument arrives mid-turn. Configuration arrives before there is a turn at all, which makes it the cheapest place in the system to be strict. Environment tags declare the names, defaults, and field types:

{{< include path="agents/go/config/config.go" region="settings-provider-fields" lang="go" >}}

Those are field-level parses: `Entrypoint` and `ModelProvider` are typed enums, `OpenAIAPIKey` is a `Secret`. But some combinations parse perfectly and still cannot work — a Gemini provider with no credential of any kind, for instance — so a second pass compares fields against each other:

{{< include path="agents/go/config/validate.go" region="settings-provider-validation" lang="go" >}}

Notice what the failures say. They do not report "invalid configuration"; they name the variable, the combination, and the two ways out of it. Here is that machinery firing for real, with two unrelated mistakes made at once:

```console
$ AGENT_ENTRYPOINT=triage AGENT_MAX_RETRIES=-1 mise run config:check
[config:check] $ go run ./cmd/agent config:check
Agent configuration is invalid:
- AGENT_ENTRYPOINT: must be one of agent, workflow, coordinator; got "triage"
- AGENT_MAX_RETRIES: must be between 0 and 10, got -1
exit status 1
[config:check] ERROR task failed
```

Both problems in one report, before a model client, a database handle, or a listener exists. A validator that stopped at the first error would have sent you round the loop twice. Run the same command with nothing overridden and it prints every resolved setting with secrets masked, which is the fastest way to answer "what does this process actually think it is configured to do".

After `config.Load` succeeds, callers receive one value and thread it explicitly into the model, server, policy, and tool constructors. There is no mutable package-level settings singleton to mutate from a test or a goroutine.

## Your turn: make the configuration fail on purpose

Types are easiest to believe in when you have watched one refuse you. This costs nothing and touches no file.

- **Mode**: `inspect` — every change lives on one command line and disappears with the shell.
- **Goal**: make the configuration boundary report two different classes of failure, and predict each message before you read it.
- **Files to touch**: none. Do not edit `.env`; a variable in front of the command overrides it for that run only.
- **Preflight**: from `agents/go`, run `mise run config:check` with nothing overridden and confirm it prints the resolved settings and exits zero.
- **Steps**: run `AGENT_ENTRYPOINT=triage mise run config:check`, then `AGENT_MODEL_PROVIDER=gemini mise run config:check`. Before each one, write down which variable the message will name and whether it will name a second.
- **Gate that proves completion**: the first run names `AGENT_ENTRYPOINT` and lists the three entrypoints it accepts; the second names `AGENT_MODEL_PROVIDER=gemini` and both of its credential routes. Each exits non-zero and starts nothing.
- **Final state**: no files changed, and a plain `mise run config:check` is valid again.

The second one is the interesting failure, because nothing about `AGENT_MODEL_PROVIDER=gemini` is malformed. It parses. It is rejected by a rule that had to look at two fields at once — which is exactly the class of bug a per-field validator ships to production.

## What you can do now

- You watched one boundary turn a hostile string, a padded string, and an unknown-but-valid string into three different, stable results with no leaked internals.
- You can name the parser that narrows each external value, and say why `ParseIncidentID` is strict where `NormalizeIncidentID` is forgiving.
- You made the configuration boundary report two failures in one run, before any runtime existed.
- You can name one property the compiler cannot prove — that an answer is true, that a tool call was well timed, or that the right human approved it — and say which kind of evidence would settle it instead ([0.2. Evidence]({{< relref "/0. Overview/0.2. Evidence.md" >}})).

Type safety is not the build passing. It is a set of specific lines, each of which you can now find, where a value stops being whatever the model felt like emitting and becomes something a downstream function is allowed to trust. That is what makes the next page's tests worth writing: there is finally something narrow enough to assert about.

Continue to [4.1. Linting]({{< relref "/4. Quality/4.1. Linting.md" >}}), where a second class of defect — the one that compiles perfectly — gets caught by a machine instead of a reviewer.
