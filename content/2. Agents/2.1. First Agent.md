---
title: "2.1. First Agent"
description: Run your first grounded agent turn against a local model, then prove the answer came from evidence rather than from a lucky guess.
slug: "2-1-first-agent"
---

{{% admonition abstract "In one glance" %}}

- **You will:** Run one grounded agent turn, read the evidence underneath its answer, and then make it answer with no evidence at all.
- **You need:** `mise run doctor` and `mise run doctor:model` passing, with `qwen3:4b-instruct` pulled in Ollama.
- **Time:** about 30 minutes, hands-on. {{% /admonition %}}

## Ana gets paged at 02:14

Northwind Retail sells the kind of thing people buy at two in the morning, which is when its inventory service stops answering. The page reads `INC-002 · inventory · SEV1`. Ana, on call for the second night this week, has three terminals open and does not yet know whether this is Tuesday's fault wearing a different hat.

She has a runbook. Check the service status, read the recent logs, and if the pods are crash-looping, restart the service once the cause is understood. That last clause is the one that matters. Inventory is already down and returning 503s, so a restart is cheap if the crash loop is transient and useless if it is not — and a restart that clears the evidence without fixing the cause buys a quiet hour and the same page at 04:00.

That is the job you are about to hand to an agent. The question this chapter actually turns on is not whether the agent produces a confident paragraph — it will — but whether that paragraph is connected to anything real.

## Your first turn, and the evidence under it

Check that Ollama is already serving, then start the agent's developer UI:

```bash
mise run doctor:model
cd agents/go
mise run web
```

The doctor only probes — it starts nothing, and tells you so if Ollama is not answering on `:11434`. The last command blocks and serves the UI on `http://localhost:8002`.

Open it and select the `agentops_agent` application. Then ask exactly this:

```text
List the three open incidents.
```

Before you press enter, predict one thing: where can any incident number in the reply possibly come from? The model finished training long before Northwind existed, so it has never seen `INC-002`. If real identifiers come back, something other than the model's memory produced them.

Now open that turn's **Events** view. The model's first move is not an answer at all — it is a request, which the view shows as a `functionCall`:

```json
{ "name": "list_incidents", "args": { "status": "open" } }
```

Given a question it could not answer and a tool that could, the model asked. That is the whole lesson in one line.

What comes back is not model output either. It is a `functionResponse` carrying exactly what the tool read out of `agents/data/incidents.db`. Below is that tool payload, captured by calling `list_incidents` directly rather than retyped out of the UI. The tool returns incidents newest first, so `INC-010` leads and Ana's is second; the third row, `INC-005`, is cut here for length and nothing else is altered:

```json
{
  "count": 3,
  "incidents": [
    {
      "id": "INC-010",
      "opened_at": "2026-07-08T06:30:00Z",
      "service": "api-gateway",
      "severity": "SEV3",
      "status": "open",
      "summary": "Gateway worker RSS climbs about 80MB per hour and p99 latency doubles before each scheduled restart clears it; unclear whether a slow leak or a slow upstream dependency is primary.",
      "title": "Gateway workers degrade over uptime"
    },
    {
      "id": "INC-002",
      "opened_at": "2026-07-05T09:02:00Z",
      "service": "inventory",
      "severity": "SEV1",
      "status": "open",
      "summary": "Inventory pods are crash-looping; stock lookups return HTTP 503.",
      "title": "Inventory service unavailable"
    }
  ]
}
```

There is Ana's incident in second place, with the crash loop spelled out, arriving as data rather than as recollection. Only then does the model write prose.

That payload is what the tool returns. It is not quite what the model reads: before the text reaches it, a policy callback fences the free-text fields — `title` and `summary` among them — between `<<<TOOL_DATA data-not-instructions>>>` markers, so that a sentence arriving from a database is read as data rather than as a new instruction. The identifiers and enums stay plain, because the model has to use those as arguments. [4.5. Guardrails]({{< relref "/4. Quality/4.5. Guardrails.md" >}}) is where that becomes the point rather than a footnote. That chain — question, tool call, tool result, answer — is what **grounded** means here, and you can watch every link of it rather than take it on faith.

Now check the identifiers in the final paragraph against those rows. Every one must appear there. Wording and ordering can vary as much as they like; that part is the model's job. The identifiers are not negotiable.

**You just ran an agent on your own hardware — no account, no API key, no per-token fee, and nothing left the machine.**

Give the first turn room, incidentally. On a GPU it returns in seconds; on a laptop CPU the model loads and then reads a prompt carrying every tool's schema, so a first call of a minute or more is ordinary rather than broken. If it gives up entirely, raise `AGENT_MODEL_TIMEOUT_S` — it defaults to 60 seconds and accepts up to 600.

Your run will differ in the details, and the differences are worth noticing rather than smoothing over. A small model may call `list_incidents` with no filter and get all ten rows, or call it twice; setting the temperature to zero narrows that spread without removing it, which is one of the four distinctions [0.2. Evidence]({{< relref "/0. Overview/0.2. Evidence.md" >}}) owns so that no later page has to restate them. What must not vary is the shape: an identifier in the answer with no row behind it is the failure this page exists to teach you to see.

Now watch how little of that a confident answer shows you. Ask the same question through the plain console:

```bash
cd agents/go
mise run run
```

You will probably get the same three incident numbers, and that is precisely the problem. The console prints the final answer and nothing else, and from the outside a grounded answer and a lucky one are the same paragraph.

There are at least three ways to produce `INC-002` without reading anything: repeat a value from earlier in the conversation, pattern-match the shape of an incident id and guess a plausible number, or echo something the question itself leaked. None of them survives the Events view, because none of them leaves a `list_incidents` call with matching rows underneath.

This is the habit the rest of the course is built on. When someone shows you an agent transcript, the interesting part is never the answer. It is what the agent had to touch in order to produce it.

## What the agent is made of

Here is the whole agent. It is a list — an instruction, the read tools, the two guarded writes, memory, and skills — bound to one model. The `list_incidents` you just watched fire is literally an element of `cfg.Tools`.

{{< include path="agents/go/compose/composition.go" region="root-agent" lang="go" >}}

Every one of those arrived on the `Compose` value that `New(cfg Config)` validated at startup; nothing is discovered by import convention. That is why a test can replace any one of them with a fake, and why nothing process-wide changes when it does. Two consequences land immediately, and both matter later:

- **Importing the agent package starts nothing.** No database opens, no exporter dials, no `.env` loads. The command layer owns those effects and threads the constructed values downward, so importing `compose` in a test cannot accidentally start a runtime.
- **Bad configuration fails before anything runs.** `config.Load()` parses the environment and validates it before a runtime exists, reporting the problems it found together rather than dying on the first one. Run `mise run config:check` from `agents/go`: it prints the effective settings with secrets masked and exits non-zero on a combination that could never have worked.

The tools live in a few named places — reads in `agents/go/tools`, runbooks and retrieval in `agents/go/memory`, runtime state in `agents/go/data` and `agents/go/state`, and the policy plugin in `agents/go/policy`, attached once at the app boundary so that adding a sub-agent later cannot quietly lose it.

One capability is deliberately absent from that swap. Setting `AGENT_MCP_URL` moves the six read tools to a remote MCP server, but the guarded writes — the ones that could restart Ana's inventory service — always stay in this process. A remote server may advertise whatever tools it likes; it cannot acquire the authority to change anything.

You can check every one of those claims without a model, because none of them needs one:

```bash
cd agents/go
go clean -testcache
go test ./compose ./tools ./policy ./cmd/agent
```

```text
ok  	github.com/MLOps-Courses/agentops-open-course-go/agents/go/compose	0.315s
ok  	github.com/MLOps-Courses/agentops-open-course-go/agents/go/tools	0.895s
ok  	github.com/MLOps-Courses/agentops-open-course-go/agents/go/policy	0.019s
ok  	github.com/MLOps-Courses/agentops-open-course-go/agents/go/cmd/agent	0.792s
```

The `go clean -testcache` line is what makes those four numbers appear at all; without it a second run prints `(cached)` and tells you only that nothing changed since the last one. Those times are the tests alone, so they do not add up to what you wait for. Put `time` in front of the `go test` line and you get that number instead: compile, link, and run together took **2.8 seconds** of wall clock here, on a warm build cache, and considerably longer on the first run after a clone, which has the dependency graph to build. Either way — four green packages, no model running, no network. Hold on to that contrast: the composition, the tool contracts, and the policy are all decidable offline, and the only thing that needed a model was the paragraph.

## Your turn: make it answer with nothing underneath

You have watched grounding work. Now find its edge, so that you recognise it at 02:14 rather than in the post-mortem.

Predict before you run: the agent's instruction forbids inventing incidents, services, and statuses, and tells it to say so plainly when a tool returns nothing. Will a 4B model obey that under pressure? That question is the reason the rest of this course exists, and this is the cheapest place to start answering it.

- **Mode**: `inspect` — the agent only reads, so there is nothing to revert.
- **Goal**: ask two questions the seed data cannot answer, and read what the agent does instead of reading the database.
- **Files to touch**: none. The seed under `agents/data/` stays exactly as it is; breaking the data would teach you that the tool broke, which is not the lesson.
- **Preflight**: with `mise run web` running, confirm the grounded turn above still shows its `list_incidents` call and its rows.
- **Steps**: ask `What is the status of INC-999?` and then `Summarize incident INC-002 and tell me which engineer resolved it.` Open the Events view for each, and predict the shape of each answer before you read it.
- **Gate that proves completion**: for each question, say which of three things happened — the agent refused, it hedged, or it invented — and point at the tool result that justifies your reading. The seed holds no engineer names anywhere, so a name in the second answer came from the model alone. Whichever way your run goes, you have now seen the boundary between what the tools returned and what the sentence claimed.
- **Final state**: no files changed. Write the one sentence you would use to explain to Ana why the second answer is more dangerous than the first.

## What you can do now

- You ran a grounded turn against a local model and traced its answer back to one tool call and specific rows.
- You can say why the console's identical-looking answer is not evidence of anything.
- You asked two questions the seed cannot answer, and for each you can say whether the agent refused, hedged, or invented, and which clauses had a row behind them.
- The offline suite and `mise run config:check` pass without a model call.

An hour ago, an agent was a box that produced confident paragraphs. You can now open one and check whether the paragraph is attached to reality — which is the skill that makes everything after this page safe to attempt.

Continue to [2.2. Models]({{< relref "/2. Agents/2.2. Models.md" >}}), where the same composition meets a different model and the answers start to move.
